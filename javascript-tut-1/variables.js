let num = 4;

console.log(num);

num = 9; //not use the key word 'let' again because 'num' has already been defined  on the first line

console.log(num);

let username = "katlego"; //always use double codes "" when assigning a text/string
let user = "katlego's car"; //always use double codes "" when assigning a text/string
let user_name = "katlego"; //snake casing rule
let userName = "katlego"; //camel casing rule

console.log(username);

console.log(user);

//variable it is something that can change.

/* variable name
>can have characters
>can have numbers except first position(1username)
>can use $ and _ symbol
*/

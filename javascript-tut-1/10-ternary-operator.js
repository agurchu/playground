let num = 8;
let result;

// if (num % 2 === 0) result = "even";
// else result = "odd";

//using ternary operator (?:)

result = num % 2 === 0 ? "even" : "odd"; //if the statement is true value after ? will be executed and if its false the value after : will be executed

console.log(result);
